# GraalVM native image configuration for Keycloak
# Disable Oracle JDBC native image feature as we exclude the Oracle driver
Args = --report-unsupported-elements-at-runtime \
       --allow-incomplete-classpath \
       -H:+ReportUnsupportedElementsAtRuntime \
       -H:Features=-oracle.jdbc.nativeimage.NativeImageFeature